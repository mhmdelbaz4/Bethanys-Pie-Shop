
@if(@ViewBag.total > 0)
{
    <li>
        <a>
            <span class="glyphicon glyphicon-shopping-cart"></span>
            <span id="cart-status">
                @ViewBag.total
            </span>
        </a>
    </li>
}